# =============================================================================
# 2계층 — verdict-service
# =============================================================================
# 판정 담당임 — policy 의 조건과 pet 의 반려동물을 받아 부를 때마다 판정함
# v0.1.0 은 캐시가 없음 (Redis · Kafka 안 씀) · 캐시는 부하를 잰 뒤에 붙임
#
# DB 를 쓰지 않으므로 datasource 를 두지 않음
# 1계층의 JPA·Flyway 값이 내려오지만 JDBC 의존성까지 걷어내 아무 일도 일어나지 않음
# =============================================================================

server:
  port: 8086
